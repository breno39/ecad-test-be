version: '2'
services:
  message-server:
    container_name: application-codetest
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
    image: codetest:latest
    ports:
      - 8080:8080
    networks:
      - spring-cloud-network
networks:
  spring-cloud-network:
    driver: bridge